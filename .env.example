# Client-side Android-restricted Google Maps key (Maps JavaScript API for the WebView).
VITE_GOOGLE_MAPS_ANDROID_API_KEY=YOUR_GOOGLE_MAPS_ANDROID_API_KEY_HERE

# Public URL used in Supabase email confirmation links.
# Android device testing uses the computer's LAN IP instead of localhost.
VITE_APP_URL=http://10.246.151.218:8080

# Server only. Never prefix with VITE and never ship this in the Android app.
GOOGLE_MAPS_ROUTES_API_KEY=YOUR_GOOGLE_MAPS_ROUTES_API_KEY_HERE